# Cleanup & Organization Summary — 2026-05-25

This file documents the grouped, batched cleanup and OpenAPI organization changes performed on 2026-05-25. It provides a human-friendly record for reviewers and release notes.

---

## Batch 1 — Docs deduplication and redirect removal
- Commit: `chore(docs): remove duplicate OpenAPI route and unused /app/docs redirect`
- Files removed/affected:
  - Deleted: `app/api/docs/openapi/route.ts` (duplicate of `app/api/docs/openapi.json/route.ts`)
  - Deleted: `app/docs/layout.tsx` (unused)
  - Deleted: `app/docs/page.tsx` (redirect to `/api-docs`, unnecessary)
  - Kept canonical OpenAPI endpoint: `app/api/docs/openapi.json/route.ts`
  - Kept Swagger UI entry: `app/api/docs/swagger.ts`
- Rationale: Maintain a single canonical OpenAPI endpoint to avoid divergence and reduce confusion. Remove unused application-level redirect pages.
- Verification: Swagger UI still points to `/api/docs/openapi.json`. No runtime references to `/app/docs` remain.

## Batch 2 — Remove obsolete debug & legacy test scripts
- Commit: `chore(scripts): remove obsolete debug and legacy test scripts`
- Files removed (representative):
  - `scripts/_apply-trigger-fix.mjs`
  - `scripts/_check-inquiry-email.mjs`
  - `scripts/_check-properties.mjs`
  - `scripts/_inspect-trigger.mjs`
  - `scripts/test-apis.ps1`, `scripts/test-apis.sh`
  - `scripts/check-filenames.js`
  - `scripts/verify-implementation.sh`
  - `scripts/verify-api-docs.js`
  - `scripts/generate-api-spec-augment.mjs`, `scripts/generate-api-spec-merge.mjs`
- Rationale: These were temporary, debug, or earlier iterations of tooling; they are superseded by the stable smoke tests and generator scripts. Removing them reduces repository noise and maintenance cost.
- Verification: Smoke tests and generator scripts still exist (`validation-smoke-test.mjs`, `swagger-smoke-test.mjs`, `generate-api-spec.mjs`). CI tasks should reference the active scripts.

## Batch 3 — Remove stale artifacts
- Files removed:
  - `scripts/swagger-smoke-results.json`
  - `scripts/swagger-smoke-latest.log`
  - `scripts/openapi-missing.json`
- Rationale: These were generated artifacts from previous runs and do not belong in source control.
- Verification: Regenerate artifacts locally if needed; no functional impact.

## Batch 4 — OpenAPI generator and regenerated spec
- Commit: `feat(openapi): normalize and expand domain tags for Swagger grouping`
- Files changed/created:
  - `lib/openapi/spec-generator.ts` (modified)
  - `lib/openapi/generated.json` (regenerated)
- Summary of changes:
  - Expanded and normalized tag descriptions (Auth, Properties, Listings, Media, Documents, Admin - Validation, Admin - Vetting, Utilities, System, etc.).
  - Added normalization logic for tag aliases to ensure endpoints are grouped by domain in Swagger UI rather than alphabetically.
  - Regenerated `lib/openapi/generated.json` to reflect the new tags and ordering.
- Rationale: Improve API discoverability by grouping related endpoints into meaningful domains/tags.
- Verification steps performed:
  1. Run `node scripts/generate-api-spec.mjs` to regenerate the spec. The generator reported `Paths: 70`, `Operations: 92`.
  2. Confirmed `lib/openapi/generated.json` contains the updated `tags` and that Swagger UI will show grouped categories.

## Notes & Follow-ups
- I intentionally did not rebase or rewrite history for earlier commits; instead, I added this documentation file to provide detailed context without altering commit history.
- Remaining untracked and modified files shown by `git status` are unrelated to the cleanup batches above (some are new docs and local work). Before any additional commit, we should review those changes and decide grouping and messages.
- Suggested next steps:
  - Run the app locally and open `/api/docs` to verify grouping visually in Swagger UI.
  - Update README or CONTRIBUTING with the canonical way to generate the OpenAPI spec (`node scripts/generate-api-spec.mjs`).
  - Optionally add a CI check to prevent generated artifacts from being committed in future.

---

Signed-off-by: Automated cleanup on behalf of the team
